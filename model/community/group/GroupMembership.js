class GroupMembership {
  constructor(id, name, admin, active, joinTime) {
    this.id = id;
    this.name = name;
    this.admin = admin;
    this.active = active;
    this.joinTime = joinTime;
  }
}

export default GroupMembership;
