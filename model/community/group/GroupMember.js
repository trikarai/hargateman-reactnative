class GroupMember {
  constructor(id, admin, joinTime, active, userId, userName) {
    this.id = id;
    this.admin = admin;
    this.joinTime = joinTime;
    this.active = active;
    this.userId = userId;
    this.userName = userName;
  }
}
export default GroupMember;
